# osca CLI（开发中）

规划中的三个命令，随参考实现一起交付：

- `osca lint`——账本纪律的机器化：ID 唯一、引用可解、零密钥、判断必有出生证据
- `osca pack`——开发态（git 仓库）打包为交付态（zip），生成 integrity 校验
- `osca load`——装载校验：binding 与部署环境比对、重建索引

当前状态：规范先行（见 `docs/OSCA-SPEC-v0.2.md`），CLI 尚未发布。诚实地说：这一目录现在只有这份 README。
