"""OSCA 运行框架 Host 参考实现（架构 §4，七组件）。

确定性常驻进程，无 LLM。当前进度（M2 周切片）：
- W1：Loader（复用 cli 装载核心）+ 注册表 + 控制通道 + 包停 ← 本周
- W2：触发表（定时器/轮询器）+ 闸门
- W3：剧集装配器
- W4：Policy 拦截器 + Connector 代理
- W5：剧集执行 + 对账器 + replay
"""

__version__ = "0.1.0"
