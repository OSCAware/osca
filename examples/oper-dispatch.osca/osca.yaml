format: osca
format_version: "0.3"
package_id: demo-oper-dispatch
name: 示例经营处置下发
entry: AGENT.md
requires:
  runtime: ">=0.1"                    # 参考实现 osca-host 当前 0.2.x；loader 装载时校验
  bindings: [OPS_DB, DISPATCH_API]    # loader 装载时与部署环境比对，缺失即报错
