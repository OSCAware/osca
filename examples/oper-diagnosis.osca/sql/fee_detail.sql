-- CON-001.拉取费用明细 的固化查询（演示占位）。
-- 纪律：查询逻辑由工程侧维护，模型只能按接口名调用，永不改写（SPEC §4）。
-- 真实实现依部署环境的库表结构而定；此处为脱敏演示。
SELECT
    单位名称,
    费用科目,
    统计周期,
    环比涨幅,
    绝对金额
FROM 合并报表_费用明细      -- 演示表名
WHERE 统计周期 = :统计周期
  AND (:费用科目 IS NULL OR 费用科目 = :费用科目);
